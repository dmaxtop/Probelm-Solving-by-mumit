test=int(input())
for i in range(test):
    tick=str(input())
    first=0
    last=0
    for j in tick[:3]:
        first+=int(j)
    for j in tick[3:]:
        last+=int(j)
    print("YES") if first==last else print("NO")
