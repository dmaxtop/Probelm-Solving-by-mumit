n,k=map(int, input().split())
roma=input().split(" ")
desired=0
for i in  roma:
    lucky=0
    for j in i:
        if j=="4" or j=="7":
             lucky+=1
    if k>=lucky:
        desired += 1
print(desired)
        
